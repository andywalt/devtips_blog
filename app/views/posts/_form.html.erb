<%= form_for @post, html: { multipart: true } do |f| %>
	<div class="form_group">
		<label for="image">Post Image</label>
		<%= f.file_field :image, class: "form-control image_adjustment" %><br>
	</div><br>

	<div class="form_group">
		<label for="title"><%= f.label :title %><br></label>
		<%= f.text_field :title, placeholder: "Make It Witty", class: "form-control" %><br>
	</div>
	<div class="form_group">
		<label for="body"><%= f.label :body %><br></label>
		<%= f.text_area :body, placeholder: "Don't Cuss", class: "form-control", rows:"10" %><br>
		<br>
	</div>
	<div class="form-actions">
		<%= f.submit class: "btn btn-default" %>
	</div>
<% end %>